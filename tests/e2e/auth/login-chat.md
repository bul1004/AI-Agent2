# login-chat.spec.ts

既存ユーザーでログインし、チャット画面に遷移できることを確認するE2E。

## 前提

- `supabase/seed.sql` が適用済みであること
- `E2E Test Team` / `e2e-owner@example.com` が存在すること

## テスト手順

1. ログインページにアクセス
2. シード済みユーザーでログイン
3. `/chat` へ遷移することを確認
4. チャットUI（履歴・入力欄）が表示されることを確認
